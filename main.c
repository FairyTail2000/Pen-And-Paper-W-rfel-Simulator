#include "functions.h"

//void spielerausgeben(char Name_des_spielers[]);
void caller();
int main () {
	caller();
	//ladespieler();
	//char temp[20] = " Rafael";
	//spielerausgeben(temp);
	return 0;
}
